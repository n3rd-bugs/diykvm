# Normalize line endings: LF in the repo for all text (shell scripts must stay LF).
* text=auto eol=lf

# Windows-only helper scripts keep CRLF.
*.ps1 text eol=crlf

# Binary
*.jpg binary
*.png binary
*.img binary
*.deb binary
